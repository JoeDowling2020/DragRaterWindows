package nokeynoshade;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.junit.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class TestServiceClient {

    private final Logger logger = LogManager.getLogger(this.getClass());

    @Test
    public void testDragQueenAPI() throws Exception {

//        Client client = ClientBuilder.newClient();
//        WebTarget target =
//                client.target("http://www.nokeynoshade.party/api/queens/all");
//        String response = target.request(MediaType.APPLICATION_JSON).get(String.class);
//        ObjectMapper mapper = new ObjectMapper();
//        mapper.disable(DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES);
//        DragQueen dragQueen = mapper.readValue(response, DragQueen.class);
//        String expectedQueenName = "Trixie Mattel";
//        String actualName = dragQueen.getName();
//        assertEquals(expectedQueenName, actualName);

//        Client client = ClientBuilder.newClient();
//        WebTarget target =
//                client.target("http://www.nokeynoshade.party/api/queens/all");
//        String response = target.request(MediaType.APPLICATION_JSON).get(String.class);
//        ObjectMapper mapper = new ObjectMapper();
//        mapper.disable(DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES);
//        List<DragQueens> dragQueens = mapper.readValue(response, new TypeReference<List<DragQueens>>(){});
//        for (:
//             ) {
//
//        }
//
//        assertEquals(expectedQueenName, actualName);
    }



}